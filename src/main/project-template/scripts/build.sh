#!/bin/bash
# Build script - compiles/packages the project.
# Customize this for the project's chosen build tool (see .claude/project_config.md).
# Examples: PyInstaller, MSBuild, CMake, npm run build.
set -e

echo "Build script not yet configured for this project."
echo "Edit scripts/build.sh to add the build steps for this project's build tool."
