#!/bin/bash
# Setup script - prepares a clean machine for development.
# Customize this for the project's chosen language/framework (see .claude/project_config.md).
# Examples: create a virtual environment, install dependencies, restore packages.
set -e

echo "Setup script not yet configured for this project."
echo "Edit scripts/setup.sh to add the environment setup steps for this project's stack"
echo "(e.g. \"pip install -r requirements.txt\", \"npm install\", \"dotnet restore\")."
