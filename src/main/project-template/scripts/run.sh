#!/bin/bash
# Run script - starts the project locally.
# Customize this for the project's chosen language/framework (see .claude/project_config.md).
# Examples: python main.py, npm start, dotnet run.
set -e

echo "Run script not yet configured for this project."
echo "Edit scripts/run.sh to add the command that starts this project."
