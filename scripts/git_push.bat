@echo off
REM This project has no remote repository configured.
REM To add a remote, run: git remote add origin <url>
REM Then update this script accordingly.
echo No remote configured for this repository.
echo To add one: git remote add origin ^<url^>
