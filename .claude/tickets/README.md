# Legacy Ticket Copies

This directory is retained only for historical copies created by the old
Claude bootstrap framework. It is not an active ticket queue.

Use the repository-level `tickets/open/` directory for active work and
`tickets/closed/` for completed tickets. Start at the repository-root
[AGENTS.md](../../AGENTS.md).
